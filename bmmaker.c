#include <stdio.h>
#include <stdlib.h>

/* APLEASE organize your thoughts
 * What you want is a simple program with can generate a bunch of SQL statements
 * taking a text as example
 * Each Bill message can have 110 chars and 72 char according to the BM formatting
 * */

void Normal_Format  (FILE * bm ) {
    // You'll read the fucking file and then put each insert into and array
    int c

}


int main () {
    /* Create a pointer variable of type FILE and the open the file with fopen()*/
    FILE *my_bm;
    my_bm = fopen("BM.txt", "r+");



}
